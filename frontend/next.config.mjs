/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Salida 'standalone' para una imagen de contenedor pequeña y autocontenida.
  output: "standalone",
};

export default nextConfig;
